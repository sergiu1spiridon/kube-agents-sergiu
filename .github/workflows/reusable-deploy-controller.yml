name: Reusable Deploy Controller

on:
  workflow_call:
    inputs:
      target_environment:
        required: true
        type: string
        description: "The GitHub environment to use for variables (e.g., autopush, staging)"
      image_tag:
        required: true
        type: string
        description: "The Docker image tag to deploy (e.g. commit SHA or 'latest')"
      checkout_sha:
        required: true
        type: string
        description: "Immutable commit SHA to check out. Must be a SHA, not a branch or tag ref: a mutable ref lets the deployed code differ from the code that passed validation."

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    environment: ${{ inputs.target_environment }}
    env:
      GCP_PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
      GCP_REGION: ${{ vars.GCP_REGION }}
      GCP_WORKLOAD_IDENTITY_PROVIDER: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
      GCP_SERVICE_ACCOUNT: ${{ vars.GCP_SERVICE_ACCOUNT }}
      GKE_CLUSTER_NAME: ${{ vars.GKE_CLUSTER_NAME }}
    steps:
      - name: Verify Env Variables
        run: |
          for var in GCP_PROJECT_ID GCP_REGION GCP_WORKLOAD_IDENTITY_PROVIDER GCP_SERVICE_ACCOUNT GKE_CLUSTER_NAME; do
            if [ -z "${!var}" ]; then
              echo "::error::Required environment variable $var is not set."
              exit 1
            fi
          done

  deploy:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      id-token: write
    environment: ${{ inputs.target_environment }}
    env:
      GCP_PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
      GCP_REGION: ${{ vars.GCP_REGION }}
      GCP_WORKLOAD_IDENTITY_PROVIDER: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
      GCP_SERVICE_ACCOUNT: ${{ vars.GCP_SERVICE_ACCOUNT }}
      GKE_CLUSTER_NAME: ${{ vars.GKE_CLUSTER_NAME }}
      npm_config_yes: "true"
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ inputs.checkout_sha }}
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version-file: "k8s-operator/go.mod"
          cache-dependency-path: "k8s-operator/go.sum"

      - name: Authenticate to Google Cloud via WIF
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3
        with:
          token_format: "access_token"
          workload_identity_provider: ${{ env.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.GCP_SERVICE_ACCOUNT }}

      - name: Authenticate to GKE
        uses: google-github-actions/get-gke-credentials@3da1e46a907576cefaa90c484278bb5b259dd395 # v3
        with:
          cluster_name: ${{ env.GKE_CLUSTER_NAME }}
          location: ${{ env.GCP_REGION }}
          project_id: ${{ env.GCP_PROJECT_ID }}

      - name: Set Image Env
        env:
          REPO_NAME: ${{ github.repository }}
          PACKAGE_NAME: "k8s-operator"
          INPUT_IMAGE_TAG: ${{ inputs.image_tag }}
        run: |
          REPO_DOWNCASE=$(echo "$REPO_NAME" | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_REPO=ghcr.io/${REPO_DOWNCASE}/${PACKAGE_NAME}" >> "$GITHUB_ENV"
          echo "IMAGE_TAG=${INPUT_IMAGE_TAG}" >> "$GITHUB_ENV"

      - name: Perform Controller Redeployment
        env:
          IMAGE_TAG: ${{ env.IMAGE_TAG }}
        run: |
          # Helm re-tags the operator image on the live release, keeping the
          # release's own values while letting the checkout's chart defaults
          # refresh (--reset-then-reuse-values; plain --reuse-values would
          # snapshot old chart defaults into the release config, shadowing
          # every later default bump). An environment without the
          # release predates the Terraform + Helm engine and has to be
          # re-installed from terraform/examples/full-install once — this
          # fails loudly rather than half-adopting objects Helm does not own.
          if ! helm status kube-agents -n kubeagents-system >/dev/null 2>&1; then
            echo "::error::No Helm release 'kube-agents' in kubeagents-system — this environment predates the Terraform + Helm engine and must be re-installed from terraform/examples/full-install before redeploys can run."
            exit 1
          fi
          echo "Redeploying controller via helm upgrade with tag: $IMAGE_TAG"
          # CRD schema changes never ride a helm upgrade (Helm's documented
          # behaviour for crds/), so apply them first.
          kubectl apply --server-side --force-conflicts -f charts/kube-agents/crds/
          ./scripts/helm_upgrade_retry.sh kube-agents charts/kube-agents -n kubeagents-system \
            --reset-then-reuse-values --set "operator.image.tag=${IMAGE_TAG}" --wait --timeout 10m
